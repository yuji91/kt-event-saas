mutation LoginOrganizer($input: OrganizerLoginInput!) {
    loginOrganizer(input: $input) {
        accessToken
        refreshToken
        expiresIn
        tenantId
        email
        role
    }
}
